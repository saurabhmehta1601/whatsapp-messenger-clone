import { db } from "@Firebase/app";
import { IUser } from "chat-app-types";
import { collection } from "firebase/firestore";

const threadIdToAllUsers = (threadId: string, users: IUser[]) => {
  const promises = [];

};

export const createGroup = async () => {};
